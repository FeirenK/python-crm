version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest
    working_directory: ~/  
    steps:
      - checkout
      - run: pwd && cat /etc/issue  && uname -a 
      - run: sudo agt install golang
      
